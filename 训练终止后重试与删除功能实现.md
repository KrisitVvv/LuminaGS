# 训练终止后重试与删除功能实现

## 🎯 功能需求

在 TrainPage.vue 中，当用户点击终止或遇到异常退出训练后，显示"重试"和"删除"按钮：

- **重试**：删除输出目录的所有文件和 ProgressPage.vue 渲染列表对应的信息，再次执行开始训练
- **删除**：仅删除上述文件，不再次执行开始训练

---

## ✅ 实现方案

### 1. 后端 API 实现（main.js）

添加了两个新的 IPC 处理器：

#### 1.1 删除输出目录 API

```javascript
// 位置：electron/main.js
ipcMain.handle('delete-output-directory', async (event, outputPath) => {
  const fs = require('fs').promises;
  
  // 检查目录是否存在
  await fs.access(outputPath);
  
  // 递归删除目录及其内容
  await fs.rm(outputPath, { recursive: true, force: true });
  
  return { success: true, message: '输出目录已删除' };
});
```

**功能**：
- ✅ 检查输出目录是否存在
- ✅ 递归删除整个目录树
- ✅ 安全的错误处理

#### 1.2 删除项目及其输出 API

```javascript
// 位置：electron/main.js
ipcMain.handle('delete-project-and-output', async (event, data) => {
  const { projectId, outputPath } = data;
  
  // 1. 删除输出目录
  if (outputPath) {
    await fs.rm(outputPath, { recursive: true, force: true });
  }
  
  // 2. 删除项目配置
  if (projectId) {
    await projectManager.deleteProject(projectId);
  }
  
  return { success: true, message: '项目及其输出已删除' };
});
```

**功能**：
- ✅ 同时删除输出目录和项目配置
- ✅ 调用 `projectManager.deleteProject()` 删除 JSON 配置文件
- ✅ 完整的错误处理

---

### 2. 前端 API 暴露（preload.js）

```javascript
// 位置：electron/preload.js
contextBridge.exposeInMainWorld('electronAPI', {
  // ... 其他 API ...
  
  // 删除项目输出目录
  deleteOutputDirectory: (outputPath) => ipcRenderer.invoke('delete-output-directory', outputPath),
  // 删除项目及其输出
  deleteProjectAndOutput: (projectId, outputPath) => ipcRenderer.invoke('delete-project-and-output', { projectId, outputPath })
});
```

---

### 3. 前端 UI 实现（TrainPage.vue）

#### 3.1 新增状态字段

```javascript
data() {
  return {
    // ... 其他字段 ...
    
    // 显示重试/删除按钮
    showRetryDeleteButtons: false
  }
}
```

#### 3.2 按钮 UI

```vue
<!-- 位置：TrainPage.vue 模板 -->
<div class="actions-section">
  <button class="start-full-btn" @click="startFullTraining" :disabled="isTraining || isBaking">
    <span class="iconify" data-icon="solar:play-bold"></span> 
    {{ isTraining || isBaking ? '训练中...' : '开始训练' }}
  </button>
  
  <div class="control-buttons">
    <button class="stop-btn" @click="stopTraining" :disabled="!isTraining && !isBaking">终止</button>
  </div>
  
  <!-- 新增：停止后的操作按钮 -->
  <div v-if="showRetryDeleteButtons" class="retry-delete-buttons">
    <button class="retry-btn" @click="retryTraining" :disabled="!currentProjectId">
      <span class="iconify" data-icon="solar:refresh-linear"></span>
      重试
    </button>
    <button class="delete-btn" @click="deleteProject" :disabled="!currentProjectId">
      <span class="iconify" data-icon="solar:trash-bin-trash-linear"></span>
      删除
    </button>
  </div>
</div>
```

#### 3.3 显示按钮的时机

在 `stopTraining()` 方法中：

```javascript
async stopTraining() {
  try {
    // ... 停止训练进程的逻辑 ...
    
    // 强制更新 UI
    this.$forceUpdate();
    console.log('[停止操作] 停止流程完成');
    
    // 显示重试/删除按钮
    this.showRetryDeleteButtons = true;
    this.addLog('训练已停止，可以选择重试或删除项目', 'warning');
    
  } catch (error) {
    console.error('[停止操作] 发生异常:', error);
    // 即使出错也强制更新状态
    this.isTraining = false;
    this.isBaking = false;
    
    // 显示错误信息并显示按钮
    this.addLog(`停止过程出错：${error.message}`, 'error');
    this.showRetryDeleteButtons = true;
  }
}
```

**触发条件**：
- ✅ 用户主动点击"终止"按钮
- ✅ 训练过程中遇到异常退出
- ✅ Baking 过程中止

---

#### 3.4 重试功能实现

```javascript
async retryTraining() {
  console.log('[重试] 开始执行重试流程');
  
  if (!this.currentProjectId) {
    this.addLog('错误：项目 ID 不存在', 'error');
    return;
  }
  
  try {
    // 1. 删除输出目录
    if (this.modelPath) {
      this.addLog(`正在删除输出目录：${this.modelPath}`, 'warning');
      const deleteResult = await window.electronAPI?.deleteOutputDirectory(this.modelPath);
      
      if (deleteResult?.success) {
        this.addLog('✓ 输出目录已删除', 'success');
      } else {
        this.addLog(`删除输出目录失败：${deleteResult?.error || '未知错误'}`, 'error');
      }
    }
    
    // 2. 重置状态
    this.showRetryDeleteButtons = false;
    this.trainingStatus = 'idle';
    this.bakingStatus = 'idle';
    this.currentIteration = 0;
    this.logs = [];
    this.bakingLogs = [];
    this.lossData = [];
    this.psnrData = [];
    this.ssimData = [];
    this.evalL1Data = [];
    this.previewImage = null;
    
    this.addLog('状态已重置，准备重新开始训练', 'info');
    
    // 3. 再次执行开始训练
    await this.startFullTraining();
    
  } catch (error) {
    console.error('[重试] 发生异常:', error);
    this.addLog(`重试失败：${error.message}`, 'error');
  }
}
```

**执行流程**：
1. 删除输出目录（`modelPath`）
2. 重置所有训练状态和图表数据
3. 清空日志
4. 重新调用 `startFullTraining()` 开始训练

---

#### 3.5 删除功能实现

```javascript
async deleteProject() {
  console.log('[删除] 开始执行删除流程');
  
  if (!this.currentProjectId) {
    this.addLog('错误：项目 ID 不存在', 'error');
    return;
  }
  
  // 确认删除
  const confirmed = confirm(`确定要删除项目 "${this.projectName}" 及其所有输出文件吗？\n\n此操作不可恢复！`);
  
  if (!confirmed) {
    this.addLog('用户取消了删除操作', 'warning');
    return;
  }
  
  try {
    // 1. 删除项目及其输出
    const deleteResult = await window.electronAPI?.deleteProjectAndOutput(
      this.currentProjectId, 
      this.modelPath
    );
    
    if (deleteResult?.success) {
      this.addLog('✓ 项目及其输出已成功删除', 'success');
      
      // 2. 重置表单和状态
      this.projectName = '';
      this.modelPath = '';
      this.sourcePath = '';
      this.checkpoint = '';
      this.currentProjectId = null;
      this.showRetryDeleteButtons = false;
      this.trainingStatus = 'idle';
      this.bakingStatus = 'idle';
      this.currentIteration = 0;
      this.logs = [];
      this.bakingLogs = [];
      this.lossData = [];
      this.psnrData = [];
      this.ssimData = [];
      this.evalL1Data = [];
      this.previewImage = null;
      
      this.addLog('表单已清空，可以创建新项目', 'info');
      
      // 3. 通知 ProgressPage 更新队列
      if (window.electronAPI?.onTrainingQueueUpdate) {
        console.log('[删除] 触发队列更新');
      }
    } else {
      this.addLog(`删除失败：${deleteResult?.error || '未知错误'}`, 'error');
    }
    
  } catch (error) {
    console.error('[删除] 发生异常:', error);
    this.addLog(`删除失败：${error.message}`, 'error');
  }
}
```

**执行流程**：
1. 弹出确认对话框
2. 删除输出目录
3. 删除项目配置文件（`.luminags/project_xxx.json`）
4. 清空所有表单字段
5. 重置训练状态
6. 触发 ProgressPage 队列更新

---

### 4. CSS 样式

```css
/* 重试/删除按钮容器 */
.retry-delete-buttons {
  display: flex;
  gap: 0.5rem;
  margin-top: 0.5rem;
  padding-top: 0.5rem;
  border-top: 1px solid #e2e8f0;
}

/* 重试按钮 */
.retry-btn {
  flex: 1;
  padding: 0.625rem 1rem;
  border-radius: 0.5rem;
  font-size: 0.875rem;
  font-weight: 500;
  border: none;
  cursor: pointer;
  transition: all 0.2s;
  background-color: #dbeafe;
  color: #1e40af;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.375rem;
}

.retry-btn:hover:not(:disabled) {
  background-color: #bfdbfe;
}

/* 删除按钮 */
.delete-btn {
  flex: 1;
  padding: 0.625rem 1rem;
  border-radius: 0.5rem;
  font-size: 0.875rem;
  font-weight: 500;
  border: none;
  cursor: pointer;
  transition: all 0.2s;
  background-color: #fee2e2;
  color: #991b1b;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.375rem;
}

.delete-btn:hover:not(:disabled) {
  background-color: #fecaca;
}
```

---

## 📊 功能对比

| 功能 | 重试 | 删除 |
|------|------|------|
| **删除输出目录** | ✅ | ✅ |
| **删除项目配置** | ❌ | ✅ |
| **重置训练状态** | ✅ | ✅ |
| **清空表单** | ❌ | ✅ |
| **重新开始训练** | ✅ | ❌ |
| **确认对话框** | ❌ | ✅ |
| **触发队列更新** | ❌ | ✅ |

---

## 🔍 使用场景

### 场景 1：训练超参数设置不当

**情况**：训练开始后发现问题（如学习率过高、迭代次数不足）

**操作**：
1. 点击"终止"按钮
2. 点击"重试"按钮
3. 修改参数
4. 系统自动删除旧的输出文件并重新开始训练

### 场景 2：数据集错误

**情况**：选择了错误的数据集路径

**操作**：
1. 点击"终止"按钮
2. 点击"删除"按钮
3. 重新选择正确的数据集路径
4. 创建新项目

### 场景 3：训练异常退出

**情况**：训练过程中遇到错误自动停止

**操作**：
1. 系统自动显示"重试"和"删除"按钮
2. 根据情况选择重试或删除

---

## 🛡️ 安全保护

### 1. 删除确认对话框

删除操作会弹出确认对话框：
```
确定要删除项目 "xxx" 及其所有输出文件吗？

此操作不可恢复！
```

### 2. 项目 ID 验证

只有在 `currentProjectId` 存在时才启用按钮：
```javascript
:disabled="!currentProjectId"
```

### 3. 错误处理

所有删除操作都有完整的错误捕获和日志记录。

---

## 📝 修改的文件

### 1. `electron/main.js`
- ✅ 添加 `delete-output-directory` IPC 处理器
- ✅ 添加 `delete-project-and-output` IPC 处理器

### 2. `electron/preload.js`
- ✅ 暴露 `deleteOutputDirectory` API
- ✅ 暴露 `deleteProjectAndOutput` API

### 3. `gs-ir-visualization/src/components/pages/TrainPage.vue`
- ✅ 添加 `showRetryDeleteButtons` 状态字段
- ✅ 添加重试和删除按钮 UI
- ✅ 实现 `retryTraining()` 方法
- ✅ 实现 `deleteProject()` 方法
- ✅ 在 `stopTraining()` 中添加显示按钮逻辑
- ✅ 添加按钮样式

---

## ✅ 测试验证

### 测试步骤

1. **启动应用**
   ```bash
   cd gs-ir-visualization
   npm run dev
   ```

2. **创建并开始训练**
   - 填写项目信息
   - 点击"开始训练"

3. **终止训练**
   - 点击"终止"按钮
   - 观察是否出现"重试"和"删除"按钮

4. **测试重试功能**
   - 点击"重试"按钮
   - 验证输出目录是否被删除
   - 验证是否重新开始训练

5. **测试删除功能**
   - 重新启动一个训练
   - 终止后点击"删除"按钮
   - 确认删除操作
   - 验证项目配置和输出目录是否都被删除
   - 验证表单是否清空

---

## 🎯 总结

通过这次实现：

1. **用户体验提升**：训练终止后可以快速重试或清理，无需手动删除文件
2. **操作安全性**：删除前有确认对话框，防止误操作
3. **状态管理完善**：自动重置所有相关状态，保持一致性
4. **集成性好**：与现有的项目管理系统无缝集成

功能已完整实现并可以使用！🎉
