# 训练参数保存机制重构说明

## 📋 核心原则

### **分离式状态管理**

将项目配置数据严格分为两类，采用不同的更新机制：

| 数据类型 | 包含字段 | 更新时机 | 更新方式 | 责任方 |
|---------|---------|---------|---------|--------|
| **静态参数** | `projectName`, `outputPath`, `sourcePath`, `iterations`, `resolution`, `evalMode`, `gamma`, `indirect`, `bound`, `occluRes`, `occlusion`, `checkpoint` | 仅在用户点击"开始训练"按钮时 | 前端调用 `saveProjectConfig()` 一次性保存 | **前端负责** |
| **动态数据** | `currentIteration`, `loss`, `psnr`, `ssim`, `previewPath`, `status`, `evalL1`, `evalL1Type` | 训练过程中每 100 次迭代（常规）或每 1000 次迭代（评估） | 后端解析 `train.py` 的 stdout，自动调用 `projectManager.updateProject()` | **后端负责** |

---

## 🎯 设计目标

### **1. 数据来源唯一性**
- ✅ 静态参数：**唯一来源** = 前端表单输入
- ✅ 动态数据：**唯一来源** = Python 训练脚本的标准输出
- ❌ 禁止混合：前端不得保存动态数据，后端不得修改静态参数

### **2. 线程安全性**
- ✅ 训练过程中，前端**完全不调用**任何写入方法
- ✅ 后端通过 IPC 单线程处理所有状态更新
- ✅ 使用原子写入 + 重试机制确保 JSON 文件安全（已有修复）

### **3. 职责清晰**
- **前端职责**：收集用户配置 → 点击开始时保存一次 → 监听输出显示日志/图表
- **后端职责**：启动训练进程 → 解析 stdout → 自动更新项目状态到 JSON

---

## 🔧 实现细节

### **前端修改（TrainPage.vue）**

#### 1. `saveProjectConfig()` 方法增强

```javascript
async saveProjectConfig() {
  // ⚠️ 重要：仅在用户点击"开始训练"按钮时调用一次
  // 训练过程中的动态数据（loss、psnr、ssim、preview 等）由后端通过解析 train.py 输出自动更新
  // 前端不得在训练过程中调用此方法保存运行时数据
  
  if (!this.currentProjectId) {
    console.log('[保存项目] 无 projectId，跳过保存');
    return;
  }
  
  // ✅ 检查：如果正在训练中，禁止保存（避免误调用）
  if (this.isTraining || this.isBaking) {
    console.warn('[保存项目] ⚠️ 训练进行中，禁止调用 saveProjectConfig');
    console.warn('[保存项目] 动态数据应由后端通过 TRAINING_STATE_UPDATE 自动更新');
    return;
  }
  
  try {
    const config = {
      // ========== 静态用户配置（仅在点击"开始训练"时保存一次）==========
      projectName: String(this.projectName || ''),
      outputPath: String(this.modelPath || ''),
      sourcePath: String(this.sourcePath || ''),
      iterations: Number(this.iterations || 30000),
      resolution: Number(this.resolution || 1),
      evalMode: Boolean(this.evalMode),
      gamma: Boolean(this.gamma),
      indirect: Boolean(this.indirect),
      bound: Number(this.bound || 1.5),
      occluRes: Number(this.occluRes || 128),
      occlusion: Number(this.occlusion || 0.8),
      checkpoint: this.checkpoint ? String(this.checkpoint) : null,
      
      // ========== 注意：以下动态字段不应在此保存 ==========
      // ❌ 不保存：currentIteration, loss, psnr, ssim, preview, logs, metrics
      // ✅ 这些字段由后端通过解析 train.py 的 stdout 自动更新到 projects.json
    };
    
    console.log('[保存项目] ✓ 准备保存用户配置（静态参数）');
    console.log('[保存项目] ⚠️ 注意：动态数据将由后端自动更新');
    
    const result = await window.electronAPI?.updateProjectConfig(
      this.currentProjectId, 
      config
    );
    
    if (result?.success) {
      console.log(`[保存项目] ✓ 已保存 ${this.currentProjectId}`);
    }
  } catch (error) {
    console.error('[保存项目] 异常:', error);
    alert('项目配置保存失败：' + error.message);
  }
}
```

#### 2. 关键改动

**删除的内容：**
```javascript
// ❌ 已移除：不再保存动态数据
- isTraining: Boolean(this.isTraining),
- isBaking: Boolean(this.isBaking),
- currentIteration: Number(this.currentIteration || 0),
- logs: this.logs.slice(-200).map(...),
- metrics: {
    lossHistory: cleanChartData(this.lossData),
    psnrHistory: cleanChartData(this.psnrData),
    ssimHistory: cleanChartData(this.ssimData),
    evalL1History: cleanChartData(this.evalL1Data)
  },
- bakingStatus, bakingProgress, bakingLogs
```

**新增的防护：**
```javascript
// ✅ 训练过程中禁止调用
if (this.isTraining || this.isBaking) {
  console.warn('[保存项目] ⚠️ 训练进行中，禁止调用 saveProjectConfig');
  return;
}
```

---

### **后端实现（main.js）**

#### 1. 解析 `TRAINING_STATE_UPDATE`（每 100 次迭代）

```javascript
trainingProcess.stdout.on('data', (data) => {
  const output = data.toString();
  
  // 解析 TRAINING_STATE_UPDATE 消息并更新项目状态
  if (projectId && output.includes('TRAINING_STATE_UPDATE:')) {
    try {
      const jsonMatch = output.match(/TRAINING_STATE_UPDATE:\s*(\{.+\})/);
      if (jsonMatch) {
        const stateData = JSON.parse(jsonMatch[1]);
        
        // ✅ 后端自动更新动态数据
        projectManager.updateProject(projectId, {
          currentIteration: stateData.iter,
          loss: stateData.loss,
          psnr: stateData.psnr,      // 可能为 null（等待 1000 次迭代评估）
          ssim: stateData.ssim,      // 可能为 null
          previewPath: stateData.preview,
          status: 'training'
        }).then(result => {
          if (result.success) {
            console.log('[Training] 项目状态已更新');
          }
        });
      }
    } catch (error) {
      console.error('[Training] 解析 TRAINING_STATE_UPDATE 失败:', error);
    }
  }
});
```

#### 2. 解析评估数据（每 1000 次迭代）

```javascript
// 解析 EVAL_TRAIN_* 或 EVAL_TEST_* 指标
const evalTrainPsnrMatch = output.match(/EVAL_TRAIN_PSNR:\s*([\d.]+)/);
const evalTestPsnrMatch = output.match(/EVAL_TEST_PSNR:\s*([\d.]+)/);

if (evalTrainPsnrMatch || evalTestPsnrMatch) {
  const psnr = evalTrainPsnrMatch ? parseFloat(evalTrainPsnrMatch[1]) : 
                                    parseFloat(evalTestPsnrMatch[1]);
  
  // ✅ 后端自动更新评估指标
  projectManager.updateProject(projectId, {
    currentIteration: iter,
    psnr: psnr,
    ssim: ssim,
    evalL1: evalL1,
    evalL1Type: evalType,  // 'test' 或 'train'
    status: 'training'
  });
}
```

---

### **Python 训练脚本（train.py）**

#### 1. 每 100 次迭代输出（常规训练）

```python
if iteration % 100 == 0:
    # 保存预览图
    preview_path = os.path.join(point_dir, f"render_{iteration}.png")
    save_image(image.clamp(0.0, 1.0), preview_path)
    
    # JSON 格式输出（用于状态持久化）
    state_update = {
        "iter": iteration,
        "loss": float(loss_str),
        "psnr": None,  # 将在 1000 次迭代时由 EVAL_TRAIN_PSNR 提供
        "ssim": None,  # 将在 1000 次迭代时由 EVAL_TRAIN_SSIM 提供
        "preview": preview_path
    }
    sys.stdout.write(f"TRAINING_STATE_UPDATE: {json.dumps(state_update)}\n")
    sys.stdout.flush()
```

#### 2. 每 1000 次迭代输出（评估）

```python
def training_report(...):
    # 计算 PSNR 和 SSIM
    psnr_test /= len(config["cameras"])
    ssim_test /= len(config["cameras"])
    
    # 增强输出格式以便 GUI 解析
    print(f"\n[ITER {iteration}] Evaluating {config['name']}: L1 {l1_test:.6f} PSNR: {psnr_test:.6f} SSIM {ssim_test:.6f}")
    
    # 前端解析用的格式化输出
    sys.stdout.write(f"EVAL_{config['name'].upper()}_L1: {l1_test:.6f}\n")
    sys.stdout.write(f"EVAL_{config['name'].upper()}_PSNR: {psnr_test:.6f}\n")
    sys.stdout.write(f"EVAL_{config['name'].upper()}_SSIM: {ssim_test:.6f}\n")
    sys.stdout.flush()
```

---

## 📊 数据流向图

```
┌─────────────────┐
│   用户输入表单   │
│ (gamma, indirect│
│  resolution 等) │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  点击"开始训练"按钮                      │
│  ↓                                       │
│  前端调用 saveProjectConfig()           │
│  ↓                                       │
│  保存静态参数到 projects.json           │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  启动 Python 训练进程                    │
│  ↓                                       │
│  train.py 每 100 次迭代输出：             │
│  TRAINING_STATE_UPDATE: {               │
│    iter, loss, psnr, ssim, preview      │
│  }                                       │
│                                          │
│  train.py 每 1000 次迭代输出：            │
│  EVAL_TRAIN_PSNR: xx.xx                 │
│  EVAL_TRAIN_SSIM: x.xxxx                │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Electron 主进程监听 stdout              │
│  ↓                                       │
│  正则解析输出                            │
│  ↓                                       │
│  调用 projectManager.updateProject()    │
│  ↓                                       │
│  自动更新动态数据到 projects.json       │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  前端监听 training-output 事件          │
│  ↓                                       │
│  解析日志显示到 Log 面板                │
│  ↓                                       │
│  更新 ECharts 图表（仅 UI 展示）         │
└─────────────────────────────────────────┘
```

---

## ✅ 验证清单

### **静态参数保存**
- [ ] 修改表单中的任意参数（如 gamma），立即触发保存
- [ ] 点击"开始训练"按钮时，检查 `projects.json` 是否包含最新值
- [ ] 训练过程中修改参数，确认**不会**保存到 JSON（被防护逻辑阻止）

### **动态数据更新**
- [ ] 训练到 100 次迭代，检查 `projects.json` 是否包含 `loss` 和 `previewPath`
- [ ] 训练到 1000 次迭代，检查 `projects.json` 是否包含 `psnr` 和 `ssim`
- [ ] 确认前端没有调用 `saveProjectConfig` 来保存这些数据

### **并发安全**
- [ ] 快速切换多个项目，检查 JSON 是否损坏（应使用原子写入 + 重试）
- [ ] 在训练过程中强制关闭应用，重启后检查 JSON 完整性

---

## 🛡️ 安全机制

### **1. 前端防护**
```javascript
// ✅ 训练中禁止保存
if (this.isTraining || this.isBaking) {
  console.warn('[保存项目] ⚠️ 训练进行中，禁止调用 saveProjectConfig');
  return;
}
```

### **2. 原子写入（后端）**
```javascript
// ✅ projectManager.saveProjects() 使用临时文件 + 原子重命名
const tempFile = this.projectsFile + '.tmp';
await fs.writeFile(tempFile, content, 'utf8');
await fs.rename(tempFile, this.projectsFile);  // 原子操作
```

### **3. 重试机制**
```javascript
// ✅ 最多重试 3 次，指数退避
for (let attempt = 1; attempt <= maxRetries; attempt++) {
  try {
    // 写入逻辑
    break;
  } catch (error) {
    if (attempt < maxRetries) {
      await new Promise(resolve => 
        setTimeout(resolve, 100 * attempt)
      );
    }
  }
}
```

### **4. 数据验证**
```javascript
// ✅ 写入后立即验证 JSON 格式
const verifyContent = await fs.readFile(tempFile, 'utf8');
JSON.parse(verifyContent);  // 抛出异常如果格式错误
```

---

## 📝 最佳实践

### **何时调用 `saveProjectConfig()`？**
✅ **正确时机：**
- 用户修改表单参数后（防抖 1 秒）
- 点击"开始训练"按钮时（立即保存）
- 页面跳转前（如果项目加载中）

❌ **错误时机：**
- 收到 `TRAINING_STATE_UPDATE` 时
- 更新 Loss/PSNR/SSIM 图表时
- 训练进程输出日志时
- 任何定时器中周期性调用

### **如何添加新的动态字段？**

1. **Python 端**：在 `train.py` 中添加输出
```python
state_update = {
    "iter": iteration,
    "loss": float(loss_str),
    "new_metric": new_value,  # ✅ 新增字段
    "preview": preview_path
}
sys.stdout.write(f"TRAINING_STATE_UPDATE: {json.dumps(state_update)}\n")
```

2. **后端解析**：在 `main.js` 中更新 `projectManager.updateProject()`
```javascript
projectManager.updateProject(projectId, {
  currentIteration: stateData.iter,
  loss: stateData.loss,
  newMetric: stateData.new_metric,  // ✅ 新增字段
  previewPath: stateData.preview
});
```

3. **前端显示**：在 `TrainPage.vue` 中监听并显示
```javascript
handleTrainingOutput(data) {
  // 解析新字段并更新 UI（仅展示，不保存）
  const newMetricMatch = line.match(/NEW_METRIC:\s*([\d.]+)/);
  if (newMetricMatch) {
    this.newMetricValue = parseFloat(newMetricMatch[1]);
  }
}
```

---

## 🔍 故障排查

### **问题 1：JSON 文件中缺少动态数据**

**检查步骤：**
1. 查看 Python 输出是否包含 `TRAINING_STATE_UPDATE:`
2. 查看 Electron 控制台是否有 `[Training] 解析到状态更新` 日志
3. 检查 `projectManager.updateProject()` 是否成功

**常见原因：**
- Python 输出被缓冲（确保 `sys.stdout.flush()`）
- 正则表达式不匹配
- IPC 通信失败

### **问题 2：静态参数未保存**

**检查步骤：**
1. 确认点击了"开始训练"按钮
2. 查看控制台是否有 `[保存项目] ✓ 已保存` 日志
3. 检查 `projects.json` 最后修改时间

**常见原因：**
- `currentProjectId` 为空
- 表单验证失败
- IPC 调用失败

### **问题 3：训练过程中意外保存**

**检查步骤：**
1. 查找控制台警告 `[保存项目] ⚠️ 训练进行中，禁止调用`
2. 定位调用堆栈，找出误调用的代码
3. 添加 `isTraining` 检查防护

---

## 📚 相关文档

- [JSON 并发写入损坏问题完整修复.md](./JSON 并发写入损坏问题完整修复.md)
- [参数保存问题根本原因与最终修复.md](./参数保存问题根本原因与最终修复.md)
- [训练完成后保存与删除功能实现.md](./训练完成后保存与删除功能实现.md)

---

## 🎉 总结

通过本次重构，我们实现了：

✅ **职责分离**：前端管静态配置，后端管动态数据  
✅ **来源唯一**：避免多处写入导致的数据不一致  
✅ **线程安全**：训练中前端完全只读，后端单线程更新  
✅ **性能优化**：减少不必要的文件 I/O  
✅ **可维护性**：清晰的数据流向，易于调试和扩展  

**核心思想**：让专业的人做专业的事 —— 前端专注交互，后端专注数据处理。
