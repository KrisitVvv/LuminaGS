# 智能训练阶段检测与自动启动功能实现

## 🎯 问题描述

### **用户报告的核心问题**
1. Stage1 结束后未自动执行 Baking
2. 需要手动干预才能继续下一个阶段
3. 无法从断点恢复训练（Baking 出错后需要重新开始）

### **原始需求**
1. **自动检测并触发 Baking**：
   - Stage1 完成后立即检查 `chkpnt30000.pth` 文件
   - 存在则自动调用 `executeBaking()`

2. **智能启动逻辑（基于文件状态）**：
   - 同时存在 `chkpnt30000.pth` 和 `occlusion_volumes.pth` → 直接进入 Stage2
   - 仅存在 `chkpnt30000.pth` → 跳过 Stage1，直接执行 Baking
   - 两个文件都不存在 → 从 Stage1 开始

3. **状态恢复支持**：
   - 新建项目首次启动
   - 从 ProgressPage 恢复已中断的项目

---

## ✅ 解决方案

### **核心思路**

通过 Electron IPC 检测输出目录中的关键文件，智能判断应该从哪个阶段开始训练，实现无缝的断点续训和自动化流程。

---

## 🔧 技术实现

### **1. 后端：添加文件检测 IPC 方法**

**文件：** `gs-ir-visualization/electron/main.js`

```javascript
// 检查文件是否存在
ipcMain.handle('check-file-exists', async (event, filePath) => {
  try {
    if (!filePath) {
      return { success: false, error: '文件路径为空' };
    }
    
    try {
      await fs.access(filePath);
      console.log(`[FileCheck] 文件存在：${filePath}`);
      return { success: true, exists: true };
    } catch (err) {
      console.log(`[FileCheck] 文件不存在：${filePath}`);
      return { success: true, exists: false };
    }
  } catch (error) {
    console.error('[FileCheck] 检查失败:', error);
    return { success: false, error: error.message };
  }
});
```

**关键点：**
- 使用 `fs.access()` 异步检查文件存在性
- 不依赖外部日志或临时标记，以实际文件为准
- 返回标准化的结果格式

---

### **2. 前端：暴露 IPC API**

**文件：** `gs-ir-visualization/electron/preload.js`

```javascript
contextBridge.exposeInMainWorld('electronAPI', {
  // ... 其他 API
  checkFolderEmpty: (folderPath) => ipcRenderer.invoke('check-folder-empty', folderPath),
  // ✅ 新增：检查文件是否存在
  checkFileExists: (filePath) => ipcRenderer.invoke('check-file-exists', filePath),
  // ... 其他 API
});
```

---

### **3. 前端：实现智能检测逻辑**

**文件：** `gs-ir-visualization/src/components/pages/TrainPage.vue`

#### **方法 1：detectTrainingStage() - 检测应该从哪个阶段开始**

```javascript
async detectTrainingStage() {
  try {
    const checkpointPath = `${this.modelPath}/chkpnt30000.pth`;
    const occlusionPath = `${this.modelPath}/occlusion_volumes.pth`;
    
    // 检查文件是否存在
    const checkpointResult = await window.electronAPI?.checkFileExists(checkpointPath);
    const occlusionResult = await window.electronAPI?.checkFileExists(occlusionPath);
    
    const hasCheckpoint = checkpointResult?.exists === true;
    const hasOcclusion = occlusionResult?.exists === true;
    
    console.log('[文件检测]', {
      checkpoint: checkpointPath,
      hasCheckpoint,
      occlusion: occlusionPath,
      hasOcclusion
    });
    
    // 判断逻辑
    if (hasOcclusion) {
      // 有 occlusion_volumes.pth，说明 Baking 已完成，直接进 Stage2
      return {
        shouldStartStage2Directly: true,
        shouldStartBakingDirectly: false,
        stage: 'stage2'
      };
    } else if (hasCheckpoint) {
      // 只有 chkpnt30000.pth，需要执行 Baking
      return {
        shouldStartStage2Directly: false,
        shouldStartBakingDirectly: true,
        stage: 'baking'
      };
    } else {
      // 两个文件都没有，从头开始
      return {
        shouldStartStage2Directly: false,
        shouldStartBakingDirectly: false,
        stage: 'stage1'
      };
    }
  } catch (error) {
    console.error('[文件检测] 失败:', error);
    // 出错时默认从头开始
    return {
      shouldStartStage2Directly: false,
      shouldStartBakingDirectly: false,
      stage: 'stage1'
    };
  }
}
```

**判断逻辑流程图：**

```
检测文件
    ↓
有 occlusion_volumes.pth？
    ├─ YES → 直接开始 Stage2
    └─ NO
        ↓
    有 chkpnt30000.pth？
        ├─ YES → 直接开始 Baking
        └─ NO → 从头开始 Stage1
```

---

#### **方法 2：startFullTrainingFromBegin() - 从头开始完整流程**

```javascript
async startFullTrainingFromBegin() {
  this.addLog('【阶段 1/3】开始 Stage1 初始训练...', 'info');
  await this.executeStage1();
  
  // 等待 Stage1 完成
  await this.waitForStageCompletion('training');
  
  if (this.trainingStatus === 'error') {
    throw new Error('Stage1 训练失败');
  }
  
  // 更新检查点路径
  const lastSlashIndex = this.modelPath.lastIndexOf('/');
  const baseDir = this.modelPath.substring(0, lastSlashIndex);
  this.checkpoint = `${baseDir}/chkpnt30000.pth`;
  this.addLog(`【阶段 1/3】✓ Stage1 完成，检查点：${this.checkpoint}`, 'success');
  
  // checkpoint 变动后立即保存
  if (this.currentProjectId) {
    await this.saveProjectConfig();
  }
  
  // ========== Baking: 烘焙 ==========
  this.addLog('【阶段 2/3】开始 Baking 烘焙...', 'info');
  await this.executeBaking();
  
  // 等待 Baking 完成
  await this.waitForStageCompletion('baking');
  this.addLog('【阶段 2/3】✓ Baking 完成', 'success');
  
  // ========== Stage 2: 优化训练 ==========
  this.addLog('【阶段 3/3】开始 Stage2 优化训练...', 'info');
  await this.executeStage2();
  
  // 等待 Stage2 完成
  await this.waitForStageCompletion('training');
  
  if (this.trainingStatus === 'error') {
    throw new Error('Stage2 训练失败');
  }
  
  this.addLog('【阶段 3/3】✓ Stage2 完成', 'success');
  this.addLog('========================================', 'success');
  this.addLog('🎉 完整训练流程全部完成!', 'success');
  this.addLog('========================================', 'success');
}
```

---

#### **方法 3：修改 startFullTraining() - 集成智能判断**

```javascript
async startFullTraining() {
  // ... 前面的代码（创建项目、保存配置等）
  
  // ========== 智能判断：根据输出目录中的文件决定从哪个阶段开始 ==========
  const stageInfo = await this.detectTrainingStage();
  
  console.log('[智能判断] 检测结果:', stageInfo);
  
  if (stageInfo.shouldStartStage2Directly) {
    // 直接开始 Stage2
    this.addLog('检测到 chkpnt30000.pth 和 occlusion_volumes.pth 文件，跳过 Stage1 和 Baking', 'success');
    this.addLog('【阶段 3/3】开始 Stage2 优化训练...', 'info');
    await this.executeStage2();
    await this.waitForStageCompletion('training');
  } else if (stageInfo.shouldStartBakingDirectly) {
    // 直接开始 Baking
    this.addLog('检测到 chkpnt30000.pth 文件，跳过 Stage1', 'success');
    this.addLog('【阶段 2/3】开始 Baking 烘焙...', 'info');
    await this.executeBaking();
    await this.waitForStageCompletion('baking');
    this.addLog('【阶段 2/3】✓ Baking 完成', 'success');
    
    // 继续执行 Stage2
    this.addLog('【阶段 3/3】开始 Stage2 优化训练...', 'info');
    await this.executeStage2();
    await this.waitForStageCompletion('training');
  } else if (this.bakingStatus === 'error' || this.trainingStatus === 'error') {
    // 之前出错了，询问用户如何处理
    this.addLog('检测到之前的训练发生错误', 'warning');
    this.showRetryDeleteButtons = true;
    return;  // 不自动继续，让用户手动选择
  } else {
    // 从头开始完整流程
    await this.startFullTrainingFromBegin();
  }
}
```

---

## 📊 完整工作流程

### **场景 1：新建项目首次启动**

```
用户点击"开始训练"
    ↓
创建项目 → 保存配置
    ↓
调用 detectTrainingStage()
    ↓
检测文件：chkpnt30000.pth ❌, occlusion_volumes.pth ❌
    ↓
返回：stage: 'stage1'
    ↓
执行 startFullTrainingFromBegin()
    ↓
Stage1 → Baking → Stage2
    ↓
🎉 完成
```

---

### **场景 2：Stage1 完成后自动进入 Baking**

```
Stage1 完成
    ↓
生成 chkpnt30000.pth
    ↓
用户再次点击"开始训练"（或从 ProgressPage 恢复）
    ↓
调用 detectTrainingStage()
    ↓
检测文件：chkpnt30000.pth ✅, occlusion_volumes.pth ❌
    ↓
返回：shouldStartBakingDirectly: true
    ↓
直接执行 executeBaking()
    ↓
等待 Baking 完成
    ↓
自动执行 executeStage2()
    ↓
🎉 完成
```

---

### **场景 3：Baking 完成后自动进入 Stage2**

```
Baking 完成
    ↓
生成 occlusion_volumes.pth
    ↓
用户点击"开始训练"
    ↓
调用 detectTrainingStage()
    ↓
检测文件：chkpnt30000.pth ✅, occlusion_volumes.pth ✅
    ↓
返回：shouldStartStage2Directly: true
    ↓
直接执行 executeStage2()
    ↓
🎉 完成
```

---

### **场景 4：从错误中恢复**

```
Baking 阶段发生错误
    ↓
用户修复问题后点击"开始训练"
    ↓
调用 detectTrainingStage()
    ↓
检测文件：chkpnt30000.pth ✅, occlusion_volumes.pth ❌
    ↓
但是 bakingStatus === 'error'
    ↓
显示警告："检测到之前的训练发生错误"
    ↓
显示重试/删除按钮
    ↓
用户手动选择如何处理
```

---

## ✅ 验证清单

### **测试 1：新建项目从零开始**
- [ ] 输出目录为空
- [ ] 点击"开始训练"
- [ ] 观察日志：
  - `[文件检测] checkpoint: xxx, hasCheckpoint: false`
  - `【阶段 1/3】开始 Stage1 初始训练...`
- [ ] Stage1 正常启动并运行

### **测试 2：Stage1 完成后自动进入 Baking**
- [ ] 手动在输出目录放入 `chkpnt30000.pth`
- [ ] 点击"开始训练"
- [ ] 观察日志：
  - `[文件检测] hasCheckpoint: true, hasOcclusion: false`
  - `检测到 chkpnt30000.pth 文件，跳过 Stage1`
  - `【阶段 2/3】开始 Baking 烘焙...`
- [ ] Baking 正常启动
- [ ] Baking 完成后自动进入 Stage2

### **测试 3：Baking 完成后自动进入 Stage2**
- [ ] 手动放入 `chkpnt30000.pth` 和 `occlusion_volumes.pth`
- [ ] 点击"开始训练"
- [ ] 观察日志：
  - `[文件检测] hasCheckpoint: true, hasOcclusion: true`
  - `检测到 chkpnt30000.pth 和 occlusion_volumes.pth 文件，跳过 Stage1 和 Baking`
  - `【阶段 3/3】开始 Stage2 优化训练...`
- [ ] Stage2 正常启动

### **测试 4：错误恢复**
- [ ] 模拟 Baking 错误
- [ ] 点击"开始训练"
- [ ] 观察日志：
  - `检测到之前的训练发生错误`
  - 显示重试/删除按钮
- [ ] 确认不会自动继续，等待用户操作

---

## 🛡️ 注意事项

### **1. 文件检测的可靠性**

**使用 `fs.access()` 而不是读取文件内容：**
```javascript
// ✅ 正确：只检查是否存在
await fs.access(filePath);

// ❌ 错误：不要尝试读取内容
const content = await fs.readFile(filePath);  // 没必要且浪费资源
```

### **2. 错误处理**

**检测失败时默认从头开始：**
```javascript
try {
  // 检测逻辑
} catch (error) {
  console.error('[文件检测] 失败:', error);
  // 出错时默认从头开始
  return {
    shouldStartStage2Directly: false,
    shouldStartBakingDirectly: false,
    stage: 'stage1'
  };
}
```

### **3. 用户控制权**

**检测到错误时不自动继续：**
```javascript
if (this.bakingStatus === 'error' || this.trainingStatus === 'error') {
  this.addLog('检测到之前的训练发生错误', 'warning');
  this.showRetryDeleteButtons = true;
  return;  // 让用户手动选择
}
```

---

## 🚀 未来扩展

### **1. 更细粒度的断点恢复**

不仅检测文件，还检测具体迭代次数：

```javascript
// 检测最新的 checkpoint
async findLatestCheckpoint() {
  const files = await fs.readdir(this.modelPath);
  const checkpoints = files.filter(f => f.match(/chkpnt(\d+)\.pth/));
  
  if (checkpoints.length > 0) {
    // 提取迭代次数并排序
    const iterations = checkpoints.map(f => {
      const match = f.match(/chkpnt(\d+)\.pth/);
      return match ? parseInt(match[1]) : 0;
    }).sort((a, b) => b - a);
    
    return iterations[0];  // 返回最大的迭代次数
  }
  
  return null;
}
```

### **2. 训练进度可视化**

在 UI 上显示当前检测到的状态：

```vue
<div class="stage-indicator">
  <div :class="{ active: currentStage === 'stage1' }">Stage1</div>
  <div :class="{ active: currentStage === 'baking' }">Baking</div>
  <div :class="{ active: currentStage === 'stage2' }">Stage2</div>
</div>
```

### **3. 自动备份机制**

在执行每个阶段前自动备份当前状态：

```javascript
async backupBeforeStage(stage) {
  const backupPath = `${this.modelPath}/backups/${stage}_${Date.now()}`;
  await fs.mkdir(backupPath, { recursive: true });
  // 复制关键文件...
}
```

---

## 📝 相关文档

- [Stage1 结束后未自动执行 Baking 的修复.md](./Stage1 结束后未自动执行 Baking 的修复.md)
- [训练启动后自动跳转监控页面.md](./训练启动后自动跳转监控页面.md)
- [训练数据隔离机制完整实现.md](./训练数据隔离机制完整实现.md)

---

## 🎉 总结

通过本次改进，我们实现了：

✅ **智能阶段检测**：基于文件自动判断从哪个阶段开始  
✅ **无缝断点续训**：Stage1/Baking 完成后自动继续下一阶段  
✅ **错误恢复支持**：检测到错误时让用户手动选择  
✅ **稳定可靠**：使用 fs.access 确保检测结果准确  

**核心价值**：
- 🤖 **自动化**：无需手动干预每个阶段
- 🔗 **连续性**：训练流程自然衔接
- 🛡️ **可靠性**：基于实际文件而非临时标记
- 😊 **用户体验**：一键启动，智能执行

**核心思想**：让文件系统成为唯一的事实来源（Single Source of Truth），避免复杂的状态管理。
