# 功能测试清单

## 测试环境准备

1. **启动应用**
   ```bash
   cd gs-ir-visualization
   npm run dev
   ```

2. **确保有可用的训练项目**
   - 准备一个已完成 Stage1 → Baking → Stage2 全流程的项目
   - 或者手动模拟完成状态

## 测试步骤

### 测试 1: 训练完成检测

**步骤**:
1. 在 TrainPage 中开始一个完整的训练流程（或模拟完成）
2. 等待 Stage2 完成
3. 观察是否出现"保存到项目列表"和"删除"按钮

**预期结果**:
- ✅ 按钮正确显示
- ✅ 按钮样式正确（保存为绿色，删除为红色）
- ✅ 日志显示"✓ 训练已全部完成，可以选择保存到项目列表或删除"

### 测试 2: 保存功能

**步骤**:
1. 点击"保存到项目列表"按钮
2. 观察控制台和日志输出
3. 确认跳转提示

**预期结果**:
- ✅ 日志显示"正在获取最新渲染图像..."
- ✅ 日志显示"正在保存到项目列表..."
- ✅ 日志显示"✓ 项目已成功保存到项目列表！"
- ✅ 弹出确认框询问是否跳转到项目列表
- ✅ 点击"确定"后跳转到 ProjectsPage

**验证点**:
```javascript
// 检查缩略图是否生成
const fs = require('fs');
const path = require('path');
const projectPath = 'E:/GraduationProject/LuminaGS/gs-ir-visualization/electron/.projects';
const projects = JSON.parse(fs.readFileSync(path.join(projectPath, 'projects.json')));
console.log(projects);  // 应该包含 thumbnailPath 字段
```

### 测试 3: ProjectsPage 数据显示

**步骤**:
1. 访问 ProjectsPage（可以通过导航或保存后跳转）
2. 观察项目列表加载情况

**预期结果**:
- ✅ 显示真实的项目数据（不是模拟数据）
- ✅ 项目名称、创建时间等信息正确
- ✅ 状态文本正确（等待中/训练中/烘焙中/已完成）
- ✅ 阶段标签正确显示（Stage1/Baking/Stage2）
- ✅ 时间格式化正确（刚刚修改/X 分钟前/X 小时前）
- ✅ 空状态和加载状态正确显示

**验证方法**:
打开浏览器控制台，查看日志：
```
[ProjectsPage] 开始加载项目列表
[ProjectsPage] ✓ 加载了 X 个项目
```

### 测试 4: ProgressPage 队列过滤

**步骤**:
1. 访问 ProgressPage
2. 查看渲染队列中的项目
3. 在 TrainPage 保存一个已完成项目
4. 等待 5 秒后再次查看 ProgressPage 队列

**预期结果**:
- ✅ 已完成项目从队列中消失
- ✅ 只显示 waiting/training/baking 状态的项目
- ✅ 控制台显示"[项目队列] 已加载 X 个项目（已过滤已完成）"

**验证方法**:
在浏览器控制台执行：
```javascript
// 查看当前队列数据
console.log(window.__VUE_DEVTOOLS_GLOBAL_HOOK__);
// 或直接查看页面上显示的项目数量
```

### 测试 5: 删除功能

**步骤**:
1. 在 TrainPage 点击"删除"按钮
2. 确认删除对话框
3. 观察删除结果

**预期结果**:
- ✅ 弹出确认对话框
- ✅ 确认后删除输出目录和项目配置
- ✅ 表单清空，可以创建新项目
- ✅ 日志显示"✓ 项目及其输出已成功删除"

### 测试 6: 边界情况

#### 6.1 无缩略图保存

**步骤**:
1. 找一个没有渲染图像的项目（或手动删除 point 目录）
2. 尝试保存

**预期结果**:
- ✅ 能正常保存，不报错
- ✅ 日志显示"⚠ 未找到渲染图像，继续保存但不包含缩略图"
- ✅ 项目列表中该项目的缩略图显示为占位符

#### 6.2 重复保存

**步骤**:
1. 对同一个项目多次点击保存

**预期结果**:
- ✅ 不会创建重复项目
- ✅ 每次保存都会更新缩略图和状态

#### 6.3 网络/性能测试

**步骤**:
1. 快速切换多个页面
2. 同时保存多个项目

**预期结果**:
- ✅ 不会出现崩溃或严重卡顿
- ✅ 数据同步正确

## 常见问题排查

### 问题 1: 保存后项目列表不显示

**可能原因**:
- projects.json文件损坏
- 项目状态未正确更新

**解决方法**:
```bash
# 检查 projects.json 内容
cd gs-ir-visualization/electron/.projects
cat projects.json
```

### 问题 2: ProgressPage 队列不更新

**可能原因**:
- 定时器未正常工作
- IPC 通信失败

**解决方法**:
- 刷新页面
- 检查控制台是否有错误日志

### 问题 3: 缩略图无法显示

**原因**: 
- 这是已知限制，需要实现文件 URL 转换

**临时方案**:
- 显示占位图是正常的

## 自动化测试脚本

### 脚本 1: 验证项目数据

```javascript
// test-projects.js
const fs = require('fs');
const path = require('path');

const projectsFile = path.join(__dirname, 'gs-ir-visualization/electron/.projects/projects.json');

try {
  const content = fs.readFileSync(projectsFile, 'utf8');
  const projects = JSON.parse(content);
  
  console.log(`✓ 找到 ${projects.length} 个项目`);
  
  projects.forEach((p, i) => {
    console.log(`\n项目 ${i + 1}:`);
    console.log(`  ID: ${p.projectId}`);
    console.log(`  名称：${p.name}`);
    console.log(`  状态：${p.status}`);
    console.log(`  阶段：${p.stage}`);
    console.log(`  缩略图：${p.thumbnailPath || '无'}`);
  });
} catch (error) {
  console.error('✗ 读取项目文件失败:', error.message);
}
```

运行：
```bash
node test-projects.js
```

### 脚本 2: 检查缩略图文件

```javascript
// test-thumbnails.js
const fs = require('fs');
const path = require('path');

const projectsFile = path.join(__dirname, 'gs-ir-visualization/electron/.projects/projects.json');
const projects = JSON.parse(fs.readFileSync(projectsFile, 'utf8'));

let count = 0;
projects.forEach(p => {
  if (p.thumbnailPath) {
    const exists = fs.existsSync(p.thumbnailPath);
    console.log(`${exists ? '✓' : '✗'} ${p.name}: ${p.thumbnailPath}`);
    if (exists) count++;
  }
});

console.log(`\n总计：${count}/${projects.length} 个项目的缩略图存在`);
```

## 测试报告模板

```markdown
## 测试报告

**测试日期**: 2026-03-13
**测试人员**: [姓名]
**测试环境**: Windows 11, Node.js v20.x

### 测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 训练完成检测 | ✅ 通过 | - |
| 保存功能 | ✅ 通过 | - |
| ProjectsPage 显示 | ✅ 通过 | 缩略图暂时显示占位符 |
| ProgressPage 过滤 | ✅ 通过 | - |
| 删除功能 | ✅ 通过 | - |
| 边界情况 | ✅ 通过 | - |

### 发现的问题

1. [描述问题]
   - 严重程度：高/中/低
   - 复现步骤：...
   - 建议修复：...

### 总体评价

[简短总结功能质量和稳定性]
```

## 下一步工作

1. **实现缩略图访问**
   - 添加 file:// URL 转换
   - 或使用静态文件服务

2. **优化用户体验**
   - 添加保存进度提示
   - 添加批量操作功能

3. **增强稳定性**
   - 添加事务支持（保存失败回滚）
   - 添加日志记录到文件

4. **性能优化**
   - 压缩缩略图大小
   - 懒加载项目列表图片
