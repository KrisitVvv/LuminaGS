# 训练参数保存问题修复说明

## 问题描述

在 TrainPage.vue 中，用户点击"开始训练"后，选择的训练参数（如 gamma、indirect）没有被正确保存到 JSON 配置文件，而是被算法运行时的固定参数覆盖了。

## 根本原因

### 问题代码位置
`gs-ir-visualization/src/components/pages/TrainPage.vue` 中的 `executeStage1()` 方法

### 问题流程

1. **用户在界面上选择参数**：
   - gamma: true
   - indirect: true
   - checkpoint: "path/to/checkpoint.pth"

2. **startFullTraining() 启动时**（第 2076 行）：
   ```javascript
   await this.saveProjectConfig();
   // ✓ 正确保存了用户的原始参数
   console.log('[完整流程] ✓ 初始状态已保存，gamma:', this.gamma, ', indirect:', this.indirect);
   ```

3. **executeStage1() 执行时**（第 2151-2152 行）：
   ```javascript
   const config = {
     // ...
     gamma: false,      // ❌ 算法强制使用 false
     indirect: false,   // ❌ 算法强制使用 false
     checkpoint: null
   };
   ```

4. **Stage1 启动后保存配置**（原第 2177 行）：
   ```javascript
   await this.saveProjectConfig();
   // ❌ 错误：将 gamma=false, indirect=false 写入了 JSON
   console.log('[Stage1] ✓ 初始状态已保存，gamma=false, indirect=false');
   ```

### 问题本质

- Stage1 训练时**算法本身**要求使用 `gamma=false, indirect=false`
- 但用户的**真实选择**应该被保留在 JSON 配置文件中
- 之前的代码在 Stage1 启动后调用 `saveProjectConfig()`，导致算法的固定参数覆盖了用户的原始选择

## 修复方案

### 修复原则

根据项目规范记忆：
> "在训练启动前必须完整保存用户界面所有训练参数到 JSON 配置文件"
> "不应该在 Stage1 运行后保存被算法修改的参数"

### 修复内容

**删除 executeStage1() 中的 saveProjectConfig() 调用**

修改前（第 2171-2179 行）：
```javascript
// 如果有 projectId，更新项目阶段并保存配置（包含 gamma=false, indirect=false）
if (this.currentProjectId) {
  await window.electronAPI?.updateProjectStage(this.currentProjectId, 'training');
  console.log(`[Stage1] 已更新项目阶段为 training, projectId: ${this.currentProjectId}`);
  // 等待一下确保状态更新后再保存
  await this.$nextTick();
  await this.saveProjectConfig();  // ❌ 错误：会覆盖用户参数
  console.log('[Stage1] ✓ 初始状态已保存，gamma=false, indirect=false');
}
```

修改后：
```javascript
// 仅更新项目阶段，不保存配置（避免覆盖用户的原始参数设置）
if (this.currentProjectId) {
  await window.electronAPI?.updateProjectStage(this.currentProjectId, 'training');
  console.log(`[Stage1] 已更新项目阶段为 training, projectId: ${this.currentProjectId}`);
  // 注意：不在这里调用 saveProjectConfig()，避免将 gamma=false, indirect=false 写入 JSON
  // 用户的真实参数已经在 startFullTraining() 中保存过了
}
```

## 验证其他阶段

### Baking 阶段（executeBaking）
- ✅ **正确**：Baking 不涉及 gamma/indirect 参数
- ✅ **正确**：保存 checkpoint 等配置是必要的

### Stage2 阶段（executeStage2）
- ✅ **正确**：Stage2 使用用户真实的 gamma/indirect 设置
- ✅ **正确**：保存配置反映了用户的真实选择

## 数据流分析

### 正确的数据流

```
用户选择参数 
  ↓
startFullTraining() 
  ↓
saveProjectConfig() ← 【立即保存用户原始参数】✓
  ↓
executeStage1() 
  ↓
启动训练 (gamma=false, indirect=false) ← 【算法要求，不影响 JSON】✓
  ↓
仅更新阶段，不保存配置 ✓
  ↓
executeBaking()
  ↓
saveProjectConfig() ← 【保存 checkpoint 更新】✓
  ↓
executeStage2()
  ↓
saveProjectConfig() ← 【保存用户 gamma/indirect 选择】✓
```

### 修复前的错误数据流

```
用户选择参数 (gamma=true, indirect=true)
  ↓
startFullTraining() 
  ↓
saveProjectConfig() ✓
  ↓
executeStage1()
  ↓
启动训练 (gamma=false, indirect=false)
  ↓
saveProjectConfig() ← ❌ 错误：覆盖了用户参数！
  ↓
JSON 中变成 gamma=false, indirect=false ❌
```

## 相关文件

- **前端 UI**: `gs-ir-visualization/src/components/pages/TrainPage.vue`
- **后端 IPC**: `gs-ir-visualization/electron/main.js`
- **项目管理**: `gs-ir-visualization/electron/projectManager.js`
- **训练脚本**: `GS-IR/train.py`

## 测试建议

1. **设置参数**：
   - gamma: ✓ 勾选
   - indirect: ✓ 勾选
   - resolution: 2

2. **开始训练**，检查 JSON 文件：
   ```json
   {
     "config": {
       "gamma": true,      // 应该是用户的真实选择
       "indirect": true,   // 应该是用户的真实选择
       "resolution": 2
     }
   }
   ```

3. **Stage1 运行期间**：
   - 实际训练使用 gamma=false, indirect=false（算法要求）
   - 但 JSON 中应该保持用户的原始选择

4. **Stage2 运行期间**：
   - 训练使用和 JSON 中保存的应该一致（gamma=true, indirect=true）

## 总结

修复的核心思想：
- ✅ **训练启动前**：立即保存用户的所有原始参数
- ✅ **Stage1 运行时**：只更新阶段状态，不调用 saveProjectConfig()
- ✅ **Baking/Stage2**：正常保存配置（这些阶段使用用户真实参数）

这样既保证了算法的正确运行（Stage1 使用固定参数），又保证了用户配置的持久化（JSON 中保存真实选择）。
