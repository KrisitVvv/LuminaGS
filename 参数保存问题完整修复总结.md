# 参数保存问题完整修复总结

## 🎯 问题概述

用户发现点击开始训练后，`checkpoint`、`gamma`、`indirect` 等参数没有正确保存到 JSON 配置文件。

## 🔍 发现的两个关键问题

### 问题 1：Stage1 运行后错误地调用了 saveProjectConfig()

**位置**: `TrainPage.vue` 第 2171-2179 行（已修复）

**问题描述**:
- Stage1 训练时算法强制使用 `gamma=false, indirect=false`
- 但在 `executeStage1()` 中调用了 `saveProjectConfig()`
- 导致算法的固定参数覆盖了用户的真实选择

**修复方案**:
```javascript
// 修改前 ❌
if (this.currentProjectId) {
  await window.electronAPI?.updateProjectStage(this.currentProjectId, 'training');
  await this.$nextTick();
  await this.saveProjectConfig();  // ❌ 会覆盖用户参数
  console.log('[Stage1] ✓ 初始状态已保存，gamma=false, indirect=false');
}

// 修改后 ✅
if (this.currentProjectId) {
  await window.electronAPI?.updateProjectStage(this.currentProjectId, 'training');
  console.log(`[Stage1] 已更新项目阶段为 training, projectId: ${this.currentProjectId}`);
  // 注意：不在这里调用 saveProjectConfig()，避免将 gamma=false, indirect=false 写入 JSON
  // 用户的真实参数已经在 startFullTraining() 中保存过了
}
```

---

### 问题 2：加载项目时字段名不匹配

**位置**: `TrainPage.vue` 第 570-581 行（已修复）

**JSON 文件实际结构**:
```json
{
  "projectId": "project_20260313_022_20260313_0224_q1b",
  "name": "project_20260313_0224",
  "outputPath": "E:\\GraduationProject\\Testoutput",
  "sourcePath": "E:\\GraduationProject\\tools\\GS-IR-main\\datasets\\nerf_real_360\\counter",
  "totalIterations": 30000,    // ← 在顶层
  "checkpoint": null,           // ← 在顶层
  "config": {
    "resolution": 8,
    "evalMode": true,          // ← 是 evalMode 不是 eval
    "gamma": false,
    "indirect": false,
    "bound": 1.5,
    "occluRes": 128,
    "occlusion": 0.01
  },
  "metrics": { ... }
}
```

**问题代码（修改前）**:
```javascript
// ❌ 错误：字段名不匹配
this.iterations = project.config?.iterations || 30000;     // JSON 中是 totalIterations（在顶层）
this.evalMode = project.config?.eval || true;              // JSON 中是 evalMode
this.resolution = project.config?.resolution || 1;         // ✓ 正确
this.gamma = project.config?.gamma || false;               // ✓ 正确
this.indirect = project.config?.indirect || false;         // ✓ 正确
```

**修复代码（修改后）**:
```javascript
// ✅ 正确：使用 ?? 操作符提供更精确的默认值处理
this.iterations = project.totalIterations || 30000;        // ✓ 从顶层读取
this.evalMode = project.config?.evalMode ?? true;          // ✓ 字段名修正
this.resolution = project.config?.resolution ?? 1;         // ✓ 保持正确
this.gamma = project.config?.gamma ?? false;               // ✓ 保持正确
this.indirect = project.config?.indirect ?? false;         // ✓ 保持正确
this.bound = project.config?.bound ?? 1.5;                 // ✓ 保持正确
this.occluRes = project.config?.occluRes ?? 128;           // ✓ 保持正确
this.occlusion = project.config?.occlusion ?? 0.8;         // ✓ 保持正确
```

**关键改进**:
1. **`iterations`**: 从 `project.config?.iterations` 改为 `project.totalIterations`（JSON 顶层字段）
2. **`evalMode`**: 从 `project.config?.eval` 改为 `project.config?.evalMode`（正确的字段名）
3. **默认值操作符**: 从 `||` 改为 `??`（空值合并运算符），避免将 `0` 或 `false` 误判为需要默认值

---

## 📊 完整数据流分析

### 保存流程（前端 → JSON）

```
TrainPage.vue saveProjectConfig()
  ↓
发送配置对象：
{
  iterations: 30000,      // 前端字段名
  evalMode: true,
  gamma: true,
  indirect: true,
  resolution: 8,
  checkpoint: "path/to.pth"
}
  ↓
projectManager.updateProjectConfig()
  ↓
字段映射（第 486 行）:
if (iterations !== undefined) 
  projectConfig.totalIterations = iterations;  // ✅ 转换为 JSON 字段名
  
if (gamma !== undefined) 
  projectConfig.config.gamma = gamma;  // ✅ 直接保存
  ↓
写入 JSON 文件:
{
  "totalIterations": 30000,
  "checkpoint": "path/to.pth",
  "config": {
    "gamma": true,
    "evalMode": true,
    ...
  }
}
```

### 加载流程（JSON → 前端）

```
JSON 文件:
{
  "totalIterations": 30000,
  "checkpoint": "path/to.pth",
  "config": {
    "gamma": true,
    "evalMode": true,
    ...
  }
}
  ↓
projectManager.loadProject()
  ↓
返回完整 project 对象
  ↓
TrainPage.vue loadProjectFromRoute()
  ↓
字段映射（第 574-581 行）:
this.iterations = project.totalIterations || 30000;     // ✅ 读取顶层字段
this.evalMode = project.config?.evalMode ?? true;       // ✅ 读取正确字段
this.gamma = project.config?.gamma ?? false;            // ✅ 直接读取
  ↓
UI 表单显示正确的值
```

---

## ✅ 验证清单

### 1. 创建新项目并开始训练

**步骤**:
1. 设置输出路径：`E:\GraduationProject\Testoutput`
2. 设置数据集路径
3. 勾选 `gamma: true`
4. 勾选 `indirect: true`
5. 设置 `resolution: 8`
6. 点击"开始训练"

**检查点**:
- ✅ 点击后立即保存用户参数到 JSON
- ✅ JSON 中 `gamma` 应为 `true`
- ✅ JSON 中 `indirect` 应为 `true`
- ✅ JSON 中 `resolution` 应为 `8`

### 2. Stage1 运行期间

**检查点**:
- ✅ 实际训练使用 `gamma=false, indirect=false`（算法要求）
- ✅ 但 JSON 中保持用户的原始选择（`gamma=true, indirect=true`）
- ✅ Stage1 不调用 `saveProjectConfig()` 覆盖参数

### 3. Stage1 完成后

**检查点**:
- ✅ checkpoint 更新为 `chkpnt30000.pth`
- ✅ 其他参数保持不变

### 4. Stage2 运行期间

**检查点**:
- ✅ 训练使用和 JSON 中保存的一致（`gamma=true, indirect=true`）
- ✅ checkpoint 正确指向 Stage1 的输出

### 5. 重新加载项目

**步骤**:
1. 关闭应用
2. 重新打开应用
3. 加载之前创建的项目

**检查点**:
- ✅ UI 表单正确显示所有参数
- ✅ `gamma` 复选框正确勾选
- ✅ `indirect` 复选框正确勾选
- ✅ `resolution` 正确显示为 `8`
- ✅ `checkpoint` 路径正确恢复

---

## 🛠️ 修改文件清单

### 1. TrainPage.vue

**修改 1**: 删除 executeStage1() 中的 saveProjectConfig() 调用
- 位置：第 2171-2179 行
- 原因：避免将算法的固定参数写入 JSON

**修改 2**: 修正 loadProjectFromRoute() 中的字段映射
- 位置：第 574-581 行
- 原因：JSON 字段名不匹配导致加载失败

---

## 🔧 技术细节

### `||` vs `??` 操作符

```javascript
// 使用 || 的问题
value = config.eval || true;  
// 如果 config.eval = false，结果会是 true ❌
// 因为 false || true = true

// 使用 ?? 的正确方式
value = config.evalMode ?? true;
// 只有当 config.evalMode = undefined/null 时才使用默认值 ✅
// false ?? true = false（正确保留 false）
```

### JSON 字段命名规范

| 前端字段名 | JSON 顶层字段 | JSON config 内字段 |
|-----------|--------------|-------------------|
| iterations | totalIterations | - |
| evalMode | - | evalMode |
| gamma | - | gamma |
| indirect | - | indirect |
| resolution | - | resolution |
| bound | - | bound |
| occluRes | - | occluRes |
| occlusion | - | occlusion |
| checkpoint | checkpoint | - |

---

## 📝 测试建议

### 自动化测试脚本

```javascript
// 测试 1：验证保存逻辑
const testSave = () => {
  const config = {
    gamma: true,
    indirect: true,
    resolution: 8
  };
  saveProjectConfig(config);
  const saved = readJSON('project.json');
  assert(saved.config.gamma === true, 'gamma 应该为 true');
  assert(saved.config.indirect === true, 'indirect 应该为 true');
  assert(saved.config.resolution === 8, 'resolution 应该为 8');
};

// 测试 2：验证加载逻辑
const testLoad = () => {
  const json = {
    totalIterations: 35000,
    config: {
      gamma: true,
      evalMode: false
    }
  };
  loadProject(json);
  assert(vm.iterations === 35000, 'iterations 应该从 totalIterations 读取');
  assert(vm.gamma === true, 'gamma 应该正确加载');
  assert(vm.evalMode === false, 'evalMode 应该正确加载');
};
```

### 手动测试场景

1. **场景 A**: 新用户首次创建项目
   - 所有参数都应该正确保存
   
2. **场景 B**: 加载已有项目
   - 所有参数都应该正确恢复
   
3. **场景 C**: Stage1 → Baking → Stage2 完整流程
   - 每个阶段的参数变更都应该正确记录

---

## 🎉 总结

通过本次修复，解决了以下核心问题：

1. ✅ **Stage1 参数覆盖问题**: 删除了 executeStage1() 中的 saveProjectConfig() 调用
2. ✅ **字段映射不一致问题**: 修正了 loadProjectFromRoute() 中的字段名映射
3. ✅ **默认值逻辑问题**: 使用 `??` 替代 `||`，避免误判 `false` 和 `0`

现在整个参数保存和加载流程已经完全正确：
- 用户的真实选择会被正确保存到 JSON
- 重新加载项目时参数会正确恢复
- Stage1 使用固定参数但不会污染 JSON 配置
- Stage2 使用用户的真实参数并正确保存

**修复完成！** 🎊
