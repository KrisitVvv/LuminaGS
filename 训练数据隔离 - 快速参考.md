# 训练数据隔离 - 快速参考

## 🎯 一句话总结

**每个项目的监控数据必须相互隔离，TrainPage 只能加载和显示当前所选项目的数据。**

---

## ✅ 核心改动

### **1. mounted() - 判断来源**

```javascript
async mounted() {
  if (this.$route.query.resumeProjectId) {
    await this.loadProjectFromRoute();  // 恢复项目
  } else {
    // 新建训练：清空所有动态数据
    this.lossData = [];
    this.psnrData = [];
    this.ssimData = [];
    this.evalL1Data = [];
    this.logs = [];
    this.bakingLogs = [];
    this.currentIteration = 0;
    this.previewImage = null;
    this.isTraining = false;
    this.isBaking = false;
    this.currentProjectId = null;
  }
}
```

### **2. loadProjectFromRoute() - 先清空再加载**

```javascript
async loadProjectFromRoute() {
  // 先清空之前的动态数据（避免跨项目污染）
  this.lossData = [];
  this.psnrData = [];
  this.ssimData = [];
  this.evalL1Data = [];
  this.logs = [];
  this.bakingLogs = [];
  this.currentIteration = 0;
  this.previewImage = null;
  
  // 只恢复静态配置
  this.projectName = project.name || '';
  this.modelPath = project.outputPath || '';
  // ...
  
  // ❌ 不恢复图表数据和日志（等待后端实时填充）
}
```

### **3. setupIPCListeners() - 添加过滤**

```javascript
setupIPCListeners() {
  window.electronAPI?.onTrainingOutput((data) => {
    if (this.currentProjectId) {
      this.handleTrainingOutput(data);  // ✓ 处理
    } else {
      console.log('[训练输出] 无 currentProjectId，忽略输出');  // ✗ 忽略
    }
  });
  
  window.electronAPI?.onBakingOutput((data) => {
    if (this.currentProjectId) {
      this.handleBakingOutput(data);  // ✓ 处理
    } else {
      console.log('[烘焙输出] 无 currentProjectId，忽略输出');  // ✗ 忽略
    }
  });
}
```

### **4. handleTrainingOutput/BakingOutput - 二次检查**

```javascript
handleTrainingOutput(data) {
  if (!this.currentProjectId) {
    console.log('[训练输出] 无 currentProjectId，忽略输出');
    return;
  }
  // ... 处理输出
}

handleBakingOutput(data) {
  if (!this.currentProjectId) {
    console.log('[烘焙输出] 无 currentProjectId，忽略输出');
    return;
  }
  // ... 处理输出
}
```

---

## 📊 场景对比

### ❌ 修复前

| 操作 | 结果 |
|-----|------|
| 后台有项目 A 在训练 | 显示 A 的数据 |
| 新建项目 B | ❌ 也显示 A 的数据（错误） |

### ✅ 修复后

| 操作 | 结果 |
|-----|------|
| 后台有项目 A 在训练 | 显示 A 的数据 |
| 新建项目 B | ✅ 显示空白（正确） |
| 启动项目 B 的训练 | ✅ 只显示 B 的数据（正确） |

---

## 🔍 关键变量

| 变量 | 作用 | 何时设置 |
|-----|------|---------|
| `currentProjectId` | 当前项目 ID | 进入页面时设置 |
| `lossData`, `psnrData` 等 | 图表数据 | 后端实时填充 |
| `logs`, `bakingLogs` | 日志数据 | 后端实时填充 |

---

## 🛡️ 三层防护

```
第一层：mounted() - 初始化时清空
    ↓
第二层：loadProjectFromRoute() - 加载时不恢复
    ↓
第三层：IPC 监听器 - 过滤非当前项目
```

---

## ✅ 验证清单

### **新建训练**
- [ ] 图表区域为空
- [ ] 日志区域为空
- [ ] 控制台显示 `[新建训练] ✓ 动态数据已清空`

### **恢复项目**
- [ ] 静态配置正确恢复
- [ ] 图表初始为空
- [ ] 训练数据实时涌入

### **多项目隔离**
- [ ] 项目 A 训练中
- [ ] 新建项目 B 不显示 A 的数据
- [ ] 启动项目 B 后数据互不干扰

---

## 📝 注意事项

### ❌ **不要恢复历史数据**

```javascript
// 错误做法
if (project.metrics) {
  this.lossData = project.metrics.lossHistory;  // ❌ 不应恢复
}

// 正确做法
console.log('[加载项目] 跳过恢复图表数据和日志');  // ✅ 保持空值
```

### ✅ **currentProjectId 是关键**

```javascript
// 所有数据处理前都要检查
if (!this.currentProjectId) {
  return;  // 直接返回
}
```

---

## 📚 详细文档

[训练数据隔离机制完整实现.md](./训练数据隔离机制完整实现.md)

---

## 🎉 核心价值

🔒 **安全性**：防止跨项目数据泄露  
🎯 **准确性**：确保数据 100% 属于当前项目  
🚀 **可维护性**：清晰的数据流向  
😊 **用户体验**：所见即所得
