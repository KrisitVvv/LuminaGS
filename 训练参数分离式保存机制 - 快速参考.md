# 训练参数分离式保存机制 - 快速参考

## 🎯 核心原则

**一句话总结**：前端管配置，后端管状态；点击开始时保存一次，训练过程自动更新。

---

## 📊 数据分类表

| 数据类型 | 字段示例 | 何时保存 | 谁负责保存 |
|---------|---------|---------|-----------|
| **静态配置** | gamma, indirect, resolution, checkpoint | 仅点击"开始训练"时一次 | 前端 `saveProjectConfig()` |
| **动态状态** | loss, psnr, ssim, iteration, preview | 每 100/1000 次迭代自动更新 | 后端解析 stdout → `updateProject()` |

---

## ✅ 修改内容

### TrainPage.vue

#### 1. 添加训练中防护
```javascript
async saveProjectConfig() {
  // ✅ 新增：训练中禁止调用
  if (this.isTraining || this.isBaking) {
    console.warn('[保存项目] ⚠️ 训练进行中，禁止调用 saveProjectConfig');
    return;
  }
  // ... 其余逻辑
}
```

#### 2. 删除动态字段保存
```javascript
const config = {
  // ✅ 只保存静态配置
  projectName: ...,
  gamma: ...,
  indirect: ...,
  checkpoint: ...,
  
  // ❌ 已删除：不再保存这些动态字段
  // - currentIteration
  // - loss, psnr, ssim
  // - logs, metrics
  // - bakingStatus, bakingLogs
};
```

#### 3. 简化代码
- 删除 `safeClone()` 工具函数（不再需要清理复杂对象）
- 删除 `cleanChartData()` 工具函数（图表数据不再保存到 JSON）

---

## 🔧 工作流程

### 阶段 1：用户配置（前端主导）

```
用户填写表单 
  → 修改参数（gamma、indirect 等）
  → 防抖 1 秒后自动保存
  → 保存到 projects.json
```

### 阶段 2：开始训练（交接点）

```
点击"开始训练"按钮
  ↓
前端调用 saveProjectConfig()  ← 最后一次保存静态参数
  ↓
启动 Python 训练进程
  ↓
控制权交给后端
```

### 阶段 3：训练过程（后端主导）

```
Python train.py
  ↓
每 100 次迭代输出：
  TRAINING_STATE_UPDATE: {"iter": 100, "loss": 0.123, ...}
  ↓
Electron 监听 stdout
  ↓
解析正则表达式
  ↓
调用 projectManager.updateProject()
  ↓
自动更新到 projects.json
```

### 阶段 4：前端显示（仅 UI 更新）

```
监听 training-output 事件
  ↓
解析日志文本
  ↓
更新 ECharts 图表（仅内存）
  ↓
显示到 Log 面板（仅 UI）
  ↓
❌ 不调用任何 save 方法
```

---

## 🛡️ 安全机制

### 前端防护
```javascript
// ✅ 训练中禁止保存
if (this.isTraining) {
  return;  // 直接返回
}
```

### 后端原子写入
```javascript
// ✅ 临时文件 + 原子重命名
await fs.writeFile(tempFile, content);
await fs.rename(tempFile, originalFile);
```

### 重试机制
```javascript
// ✅ 最多 3 次重试
for (let attempt = 1; attempt <= 3; attempt++) {
  try { /* 写入 */ break; }
  catch { /* 等待后重试 */ }
}
```

---

## 📝 代码位置

| 功能 | 文件 | 关键代码段 |
|-----|------|-----------|
| 前端保存配置 | `TrainPage.vue` | `saveProjectConfig()` 方法 |
| 后端解析输出 | `main.js` | `trainingProcess.stdout.on('data')` |
| Python 输出 | `train.py` | `TRAINING_STATE_UPDATE` 和 `EVAL_*` 输出 |
| 项目管理 | `projectManager.js` | `updateProject()` 方法 |

---

## 🔍 调试技巧

### 查看静态参数是否保存
```bash
# 点击"开始训练"后立即检查
cat .projects/projects.json | jq '.[0]'
# 应包含：gamma, indirect, resolution, checkpoint
```

### 查看动态数据是否更新
```bash
# 训练到 100 次迭代后检查
cat .projects/projects.json | jq '.[0].currentIteration'
# 应为：100
cat .projects/projects.json | jq '.[0].loss'
# 应为：具体数值
```

### 查看前端是否误调用
```javascript
// 打开 DevTools Console
// 如果看到警告：
[保存项目] ⚠️ 训练进行中，禁止调用 saveProjectConfig
// 说明有代码误调用，需要排查
```

---

## ⚠️ 常见错误

### ❌ 错误 1：在训练过程中保存动态数据
```javascript
// 错误示例
handleTrainingOutput(data) {
  const loss = parseLoss(data);
  await this.saveProjectConfig();  // ❌ 错误！不应调用
}

// 正确做法：后端自动处理，前端只更新 UI
```

### ❌ 错误 2：在 saveProjectConfig 中保存 iteration
```javascript
// 错误示例
const config = {
  gamma: this.gamma,
  currentIteration: this.currentIteration  // ❌ 不应保存
};

// 正确做法：只保存静态配置
```

---

## 📚 详细文档

完整说明请查看：[训练参数保存机制重构说明.md](./训练参数保存机制重构说明.md)
