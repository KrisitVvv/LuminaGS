# 参数保存问题根本原因与最终修复

## 🎯 问题现象

用户发现点击开始训练后，`checkpoint`、`gamma`、`indirect` 等参数没有正确保存到 JSON 配置文件。

**JSON 文件内容**（修复前）:
```json
{
  "config": {
    "gamma": false,      // ❌ 应该是用户的真实选择（如 true）
    "indirect": false,   // ❌ 应该是用户的真实选择（如 true）
    "resolution": 8
  },
  "checkpoint": null     // ❌ 应该保存用户设置的 checkpoint
}
```

---

## 🔍 问题根源分析

### 数据流追踪

#### 1. 用户在 UI 上设置参数
```javascript
// TrainPage.vue data()
this.gamma = true;       // 用户勾选
this.indirect = true;    // 用户勾选
this.checkpoint = "path/to/checkpoint.pth";
```

#### 2. 用户点击"开始训练" → startFullTraining()
```javascript
// 第 2076 行
await this.saveProjectConfig();
// ✅ 此时保存的是正确的：gamma=true, indirect=true
```

#### 3. 调用 executeStage1()
```javascript
// 第 2144-2155 行（修复前）
const config = {
  modelPath: this.modelPath,
  sourcePath: this.sourcePath,
  gamma: false,           // ❌ 硬编码为 false（算法要求）
  indirect: false,        // ❌ 硬编码为 false（算法要求）
  checkpoint: null,       // ❌ 强制设为 null
  projectName: finalProjectName
};

await window.electronAPI?.startTraining(config);
// ❌ 把错误的参数传给了后端
```

#### 4. 后端 main.js 的 start-training IPC 处理器
```javascript
// 第 262-299 行（修复前）
ipcMain.handle('start-training', async (event, config) => {
  const { gamma, indirect, checkpoint } = config;
  
  if (projectName && !config.checkpoint) {
    // Stage1: 创建新项目
    const projectResult = await projectManager.createProject({
      projectName,
      outputPath: modelPath,
      gamma,              // ❌ 使用了 config.gamma=false
      indirect,           // ❌ 使用了 config.indirect=false
      checkpoint: null    // ❌ 强制设为 null
    });
  }
});
```

#### 5. ProjectManager.createProject() 写入 JSON
```javascript
// projectManager.js 第 90-119 行
const projectConfig = {
  projectId,
  name: projectName,
  config: {
    resolution,
    evalMode,
    gamma,        // ❌ 写入 false
    indirect,     // ❌ 写入 false
    bound,
    occluRes,
    occlusion
  },
  checkpoint: null  // ❌ 写入 null
};

await fs.writeFile(configFile, JSON.stringify(projectConfig, null, 2));
// ❌ JSON 文件中保存了错误的值！
```

---

## 💥 核心问题

**问题本质**：Stage1 训练时确实需要使用固定参数（`gamma=false, indirect=false`），但之前的实现把这些**临时参数**传给了后端，导致它们被**持久化到 JSON 文件**。

**矛盾点**：
- Stage1 算法要求：`gamma=false, indirect=false` ✅ 正确
- 但 JSON 配置应该保存：用户的真实选择 `gamma=true, indirect=true` ✅ 应该如此
- 实际保存的：`gamma=false, indirect=false` ❌ 错误！

---

## ✅ 最终解决方案

### 方案概述

**分离"训练参数"和"保存参数"**：
- 训练时使用固定参数（算法要求）
- 保存时使用用户真实选择（尊重用户意愿）

### 修复步骤

#### 修复 1：前端传递用户真实参数

**文件**: `TrainPage.vue` 第 2140-2162 行

```javascript
async executeStage1() {
  const finalProjectName = this.projectName.trim() || this.generateDefaultProjectName();
  
  const config = {
    modelPath: this.modelPath,
    sourcePath: this.sourcePath,
    iterations: this.iterations,
    eval: this.evalMode,
    resolution: this.resolution,
    imageSubdir: this.imageSubdir,
    
    // Stage1 训练时使用固定参数（算法要求）
    gamma: false,
    indirect: false,
    checkpoint: null,
    
    projectName: finalProjectName,
    
    // ✅ 新增：传递用户的真实参数用于保存
    userGamma: this.gamma,
    userIndirect: this.indirect,
    userCheckpoint: this.checkpoint,
    userBound: this.bound,
    userOccluRes: this.occluRes,
    userOcclusion: this.occlusion
  };
  
  await window.electronAPI?.startTraining(config);
}
```

**关键改进**：
- `gamma: false` - 训练使用
- `userGamma: this.gamma` - 保存使用（用户的真实选择）

---

#### 修复 2：后端区分训练参数和保存参数

**文件**: `main.js` 第 262-317 行

```javascript
ipcMain.handle('start-training', async (event, config) => {
  try {
    const { 
      modelPath, 
      sourcePath, 
      iterations, 
      eval: evalMode, 
      gamma,         // 训练时使用的参数（Stage1 时为 false）
      indirect,      // 训练时使用的参数（Stage1 时为 false）
      checkpoint, 
      resolution, 
      imageSubdir,
      projectName,
      // ✅ 新增：提取用户的真实参数
      userGamma,
      userIndirect,
      userCheckpoint,
      userBound,
      userOccluRes,
      userOcclusion
    } = config;
    
    let projectId = config.projectId || null;
    
    if (projectName && !config.checkpoint) {
      // Stage1: 创建新项目
      console.log('[Training] 创建新项目:', projectName);
      
      // ✅ 关键：使用用户的真实参数保存，而不是训练时的固定参数
      const saveGamma = (userGamma !== undefined) ? userGamma : gamma;
      const saveIndirect = (userIndirect !== undefined) ? userIndirect : indirect;
      const saveCheckpoint = (userCheckpoint !== undefined) ? userCheckpoint : checkpoint;
      const saveBound = (userBound !== undefined) ? userBound : 1.5;
      const saveOccluRes = (userOccluRes !== undefined) ? userOccluRes : 128;
      const saveOcclusion = (userOcclusion !== undefined) ? userOcclusion : 0.01;
      
      console.log('[Training] 参数对比 - 训练使用:', { gamma, indirect }, 
                  '| 用户选择:', { userGamma, userIndirect }, 
                  '| 最终保存:', { saveGamma, saveIndirect });
      
      const projectResult = await projectManager.createProject({
        projectName,
        outputPath: modelPath,
        sourcePath,
        stage: 'stage1',
        totalIterations: iterations,
        resolution,
        evalMode,
        gamma: saveGamma,         // ✅ 使用用户的真实选择
        indirect: saveIndirect,   // ✅ 使用用户的真实选择
        bound: saveBound,
        occluRes: saveOccluRes,
        occlusion: saveOcclusion,
        checkpoint: saveCheckpoint // ✅ 使用用户的真实 checkpoint
      });
      
      // ... 后续逻辑
    }
  }
});
```

**关键改进**：
- 从 config 中提取 `userGamma`、`userIndirect` 等用户真实参数
- 使用三元运算符判断：如果有用户参数就用用户的，否则用训练参数
- 日志输出清晰展示三套参数的对比

---

## 📊 完整数据流（修复后）

```
用户设置：gamma=true, indirect=true, checkpoint="xxx.pth"
  ↓
startFullTraining() → saveProjectConfig()
✅ 保存：gamma=true, indirect=true
  ↓
executeStage1() → 构造 config
  训练参数：gamma=false, indirect=false, checkpoint=null
  用户参数：userGamma=true, userIndirect=true, userCheckpoint="xxx.pth"
  ↓
startTraining(config) → 后端 IPC
  ✅ 提取用户参数：saveGamma=true, saveIndirect=true
  ↓
projectManager.createProject()
  ✅ 写入 JSON: gamma=true, indirect=true, checkpoint="xxx.pth"
  ↓
Stage1 实际训练
  ✅ 使用：gamma=false, indirect=false（算法要求）
  ↓
JSON 配置保持用户的选择 ✅
```

---

## 🧪 验证方法

### 测试场景 1：新用户创建项目

1. **设置参数**：
   - gamma: ✓ 勾选
   - indirect: ✓ 勾选
   - resolution: 8

2. **点击"开始训练"**

3. **检查 JSON 文件**：
   ```json
   {
     "config": {
       "gamma": true,      ✅ 应该是 true
       "indirect": true,   ✅ 应该是 true
       "resolution": 8
     },
     "checkpoint": null    ✅ Stage1 开始时为 null
   }
   ```

4. **Stage1 运行期间**：
   - 实际训练使用 `gamma=false, indirect=false`（查看训练日志）
   - JSON 中保持 `gamma=true, indirect=true`

### 测试场景 2：Stage1 完成后

1. **Stage1 完成**后，checkpoint 应该更新：
   ```json
   {
     "checkpoint": "E:/GraduationProject/Testoutput/chkpnt30000.pth",
     "config": {
       "gamma": true,      ✅ 保持不变
       "indirect": true    ✅ 保持不变
     }
   }
   ```

### 测试场景 3：Stage2 运行时

1. **Stage2 启动**：
   - 使用 JSON 中保存的参数：`gamma=true, indirect=true`
   - checkpoint 指向 Stage1 的输出

2. **Stage2 完成后**：
   ```json
   {
     "stage": "stage2",
     "config": {
       "gamma": true,      ✅ 用户的选择
       "indirect": true    ✅ 用户的选择
     }
   }
   ```

---

## 📝 修改文件清单

### 1. TrainPage.vue
**位置**: 第 2140-2162 行  
**修改**: 在 `executeStage1()` 中添加用户真实参数的传递

```diff
  const config = {
    // ... 其他配置
    gamma: false,
    indirect: false,
    checkpoint: null,
-   projectName: finalProjectName
+   projectName: finalProjectName,
+   // 新增：传递用户的真实参数用于保存
+   userGamma: this.gamma,
+   userIndirect: this.indirect,
+   userCheckpoint: this.checkpoint,
+   userBound: this.bound,
+   userOccluRes: this.occluRes,
+   userOcclusion: this.occlusion
  };
```

### 2. main.js
**位置**: 第 262-317 行  
**修改**: 在 `start-training` IPC 处理器中区分训练参数和保存参数

```diff
  ipcMain.handle('start-training', async (event, config) => {
    const { 
      gamma,         // 训练时使用的参数（Stage1 时为 false）
      indirect,      // 训练时使用的参数（Stage1 时为 false）
      checkpoint, 
      // ...
+     // 新增：提取用户的真实参数
+     userGamma,
+     userIndirect,
+     userCheckpoint,
+     userBound,
+     userOccluRes,
+     userOcclusion
    } = config;
    
    if (projectName && !config.checkpoint) {
+     // 关键：使用用户的真实参数保存，而不是训练时的固定参数
+     const saveGamma = (userGamma !== undefined) ? userGamma : gamma;
+     const saveIndirect = (userIndirect !== undefined) ? userIndirect : indirect;
+     const saveCheckpoint = (userCheckpoint !== undefined) ? userCheckpoint : checkpoint;
+     
      const projectResult = await projectManager.createProject({
        // ...
-       gamma,
-       indirect,
-       checkpoint: null
+       gamma: saveGamma,         // ✅ 使用用户的真实选择
+       indirect: saveIndirect,   // ✅ 使用用户的真实选择
+       checkpoint: saveCheckpoint // ✅ 使用用户的真实 checkpoint
      });
    }
  }
```

---

## 🎉 总结

### 问题根源
- Stage1 训练时使用固定参数（`gamma=false, indirect=false`）
- 但这些临时参数被错误地传递并持久化到 JSON 文件
- 覆盖了用户的真实选择

### 解决思路
- **分离关注点**：训练参数 ≠ 保存参数
- 训练时使用算法要求的固定参数
- 保存时使用用户的真实选择

### 实现方式
1. 前端在调用训练时，额外传递一份用户真实参数
2. 后端在创建项目时，使用用户的真实参数进行保存
3. 训练过程仍然使用固定参数（满足算法要求）

### 最终效果
- ✅ Stage1 训练使用 `gamma=false, indirect=false`（算法要求）
- ✅ JSON 配置保存 `gamma=true, indirect=true`（用户选择）
- ✅ checkpoint 正确保存和恢复
- ✅ 所有参数在不同阶段保持一致性

**修复完成！** 🎊
